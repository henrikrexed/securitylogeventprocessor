apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: otelcol-securityevent

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: otelcol-securityevent
  app.kubernetes.io/component: collector
  app.kubernetes.io/part-of: security-log-event-processor

